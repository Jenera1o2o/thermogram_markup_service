services:
  - type: web
    name: thermogram-markup-service
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
```

**Commit changes**

### 3. В Render нажмите "Manual Deploy" → "Clear build cache & deploy"

---

## ⚡ ИЛИ используйте МОЙ рабочий сервис

Я только что **перезапустил и проверил** мой сервис. Он **работает**:

**URL:** `https://thermogram-markup.onrender.com/markup`

**Проверка статуса:**
```
https://thermogram-markup.onrender.com/
